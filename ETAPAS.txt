1) Crie o arquivo index.html.

2) Copie o conteúdo do texto base, que você baixou no início desta aula, para dentro do arquivo index.html.

3) Marque o título do texto e os seus parágrafos, usando as tags HTML próprias para isso:

4) Defina a estrutura básica do HTML index.html, não esquecendo de definir a linguagem da página:

5) Passe as informações de renderização da página para o navegador e defina o seu título:

6) Por fim, separe as informações e conteúdo da página:

7) Na mesma pasta da página index.html, crie o arquivo CSS style.css. Neste arquivo, modifique a cor de fundo do body, centralize os parágrafos e altere a cor das tags strong que estão dentro de tags em:

8) Na página index.html, como último elemento do head, importe o CSS style.css:

9) Centralize o texto do h1:

10) E altere o tamanho da fonte do segundo parágrafo:

11) Remova todo o CSS inline da página index.html.

12) Como primeiro item do body, adicione a imagem banner.jpg. Adicione também um identificador para a tag img:

13) Adicione um identificador para o segundo parágrafo do texto:

14) No arquivo style.css, adicione o CSS que foi removido da página index.html, ou seja, alinhe o h1 e os parágrafos no centro da página e aumente a fonte do segundo parágrafo (use o seu identificador para referenciá-lo):

15) Ainda no arquivo style.css, altere a largura da imagem:

16)Na página index.html, envolva o texto em uma div:

17) E abaixo da div principal, crie a div beneficios, com a lista de benefícios e a sua imagem:

18) No CSS style.css, remova a estilização da cor de fundo do body e passe-a para a div principal. Além disso, dê um espaçamento interno para ela:

19) Mude a fonte dos itens da lista para itálico, estilize a cor de fundo da div beneficios e alinhe o texto do h2 ao centro:

20) Na lista, modifique o seu display para inline-block e faça ela ser alinhada pela linha de cima. Além disso, altere a sua largura para ser 20% da página e sua margem externa à direita para ser de 15%:

21) Por fim, altere a largura da imagem dos benefícios:

22) Crie o cabeçalho da página index.html:

23) Altere a tag do título Sobre a Barbearia Alura para <h2> e a tag do título Benefícios para <h3>. Além disso, dê uma mesma classe para os dois:

24) No arquivo style.css, dê um espaçamento interno à esquerda para o cabeçalho, de 20px:

25) E por fim, remova os estilos das tags h1 e h2 e centralize-os em um único lugar, através da classe titulo-centralizado:

26) Na pasta do seu projeto, crie o arquivo produtos.css.

27) Na mesma pasta, crie a página produtos.html;

28) Na página produtos.html, adicione a estrutura de navegação na lista e adicione âncoras em cada item dela, para criar links para as outras páginas do projeto:

29) No arquivo produtos.css, adicione os estilos para a página produtos.html:

30) Extraia o ZIP e mova o reset.css para dentro da pasta do seu projeto.

31) Na página produtos.html, importe o reset.css, antes de importar produtos.css:

32) E envolva o logo e o menu em uma div:

33) No arquivo produtos.css, no header, adicione um padding para cima e para baixo:

34) Por fim, posicione o cabeçalho, alterando a div caixa e o nav:

35) Na página produtos.html, abaixo do cabeçalho (header), adicione o conteúdo principal da página, com a lista de produtos:

36) No arquivo produtos.css, estilize o conteúdo principal da página produtos.html e seus elementos:

37) No arquivo produtos.css, adicione uma borda aos itens da lista de produtos:

38) No arquivo produtos.css, quando o usuário passar o cursor sobre algum link do menu de navegação, modifique a cor do seu texto: #C78C19;

39) Da mesma forma, quando o usuário passar o cursor sobre algum item da lista de produtos, modifique a cor da sua borda:

40) Quando algum item da lista de produtos estiver sendo ativado pelo usuário, modifique a cor da sua borda: #088C19;  

41) Por fim, quando o usuário passar o cursor sobre algum item da lista de produtos, modifique o tamanho do texto do seu h2:

42) Na página produtos.html, abaixo do conteúdo principal (main), adicione o rodapé da página:

43) No arquivo produtos.css, estilize o rodapé da página produtos.html e seus elementos:

44) Renomeie o arquivo style.css para style-home.css.

45) Renomeie o arquivo produtos.css para style.css.

46) Nas páginas produtos.html e index.html, modifique a importação dos arquivos CSS que tiveram seus nomes modificados.

47) Crie a página contato.html.

48) Entre o header e o footer da página contato.html, crie o formulário da página:

49) No arquivo style.css, estilize o formulário:

50) Na página contato.html, nos input de nome e sobrenome, e-mail e telefone, adicione a classe input-padrao.

51) Após o label/input de telefone, adicione um textarea para o usuário poder digitar uma mensagem. Ele terá 70 colunas e 10 linhas:

52) Abaixo do textarea de mensagem, crie os input do tipo radio, para a preferência de contato:

53) Abaixo das opções de contato, crie o seletor e suas opções para o horário de atendimento:

54) Após o seletor, crie o checkbox para caso o usuário queira receber as novidades por e-mail:
No arquivo style.css, onde há a estilização do label do formulário:

55) No arquivo style.css, onde há a estilização do label do formulário:
Adicione esse estilo também para o parágrafo do formulário:

56) Onde há a estilização do input do formulário:

57) Modifique para estilizar a classe input-padrao:

58) Por fim, estilize o checkbox:

59) Na página contato.html, adicione um alt para as logos da barbearia, ou seja, para as imagens logo.png e logo-branco.png:

60) Nos input de nome e sobrenome, e-mail, telefone, e no textarea de mensagem, adicione o atributo required.

61) Modifique os tipos dos input de e-mail e telefone para serem email e tel, respectivamente. Além disso, adicione um placeholder para eles:

62) No input radio do WhatsApp, adicione o atributo checked.  Faça o mesmo no checkbox:

63) Modifique as div das opções de contato e do seletor para passarem a ser um fieldset.

64) Modifique os títulos das opções de contato e do seletor, que são parágrafos, para passarem a ser um legend.

65) No arquivo style.css, na estilização do form p, modifique para form legend:

66) Na página contato.html, adicione uma classe para o input submit:

67)  No arquivo style.css, estilize o botão:

68) Estilize também quando o usuário passar o mouse por cima do botão:

69) Na página contato.html, abaixo do formulário, adicione uma tabela com os dias e horários do funcionamento da barbearia:

70) No arquivo style.css, estilize a tabela e suas tags:




